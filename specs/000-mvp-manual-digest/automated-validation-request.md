# Automated Validation Request

Implement an automated validation harness for the MVP. Run `/daily-digest` against the benchmark inputs in `specs/000-mvp-manual-digest/benchmark.md`, verify the generated output matches `specs/000-mvp-manual-digest/contracts/digest-output-schema.md`, and report pass/fail for:

- structure
- count rules
- evidence presence
- low-signal handling
- benchmark expectations

Do not rely on manual review.

